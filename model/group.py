class Group:

    def __init__(self, name, header, footer):
        # конструктор записывает значение параметра в свойство объекта
        self.name = name
        self.header = header
        self.footer = footer
